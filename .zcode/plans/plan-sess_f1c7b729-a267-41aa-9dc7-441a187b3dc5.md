修复前端 JS 渲染的分类标题断链:

1. `site/template.html:1291`:分类标题链接改为 `href="${esc(data.base)}${esc(data.urlPath)}${id}/"`。
2. `scripts/build-site.mjs` 的 `inlineByLoc` 数据新增 `base: ORIGIN` 字段。
3. 运行 `node scripts/build-site.mjs` 重新生成、`node scripts/check-order.mjs` 校验。
4. 以 sensen 提交并推送 main,触发 Pages 重新部署。
5. curl 验证根页、`en/`、`ui/`、`en/ui/`、`feed.xml`、`logo.png`、`plugins.json` 返回 200;若子路径仍 404(与代码无关),再查 Settings → Pages 源配置(应为 main 分支 /docs 目录)。